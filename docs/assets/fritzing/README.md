# Fritzing Assets

This folder is reserved for optional Fritzing source files and exported wiring images.

The public repo already includes maintained SVG assets for wiring and pinout:

- `../nfc-garage-position-sensor-wiring.svg`
- `../nfc-garage-position-sensor-pinout.svg`

If a future revision adds a real Fritzing source, store it here together with an exported image so both the editable source and the rendered output stay in sync.
